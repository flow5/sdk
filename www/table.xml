<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">  
  <meta>  
    <sampleQuery>select * from {table} where lat='37.774484' and lng='-122.42009100000001';</sampleQuery>  
  </meta>  
  <bindings>  
    <select itemPath="" produces="XML">  
      <inputs>  
        <key id='lat' type='xs:string' paramType='variable' required="true" />  
        <key id='lng' type='xs:string' paramType='variable' required="true" />  
      </inputs>  
      <execute><![CDATA[ 
	
		y.rest("https://maps.googleapis.com", function (result) {
					response.object = {result: result.response}; 		
				})
				.path("maps")
				.path("api")
				.path("place")
				.path("search")
				.path("json")
				.query("sensor", false)
				.query("location", lat + ',' + lng)
				.query("radius", 500)
				.query("key", "AIzaSyBoEhihE9NoNvExtbuWL98bIQZHTrzOqbg").get();		
		  y.sync();
		
      ]]></execute>  
    </select>  
  </bindings>  
</table>